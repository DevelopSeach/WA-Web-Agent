# Codex Notes

- Runtime entry point: `npm run start` from repo root
- Production build: `npm run build` from repo root
- DB bootstrap: `npm run db:init`
- DB seed: `npm run db:seed`
- Root `.env` is the install source of truth for server and client-facing values
